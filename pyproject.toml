[tool.ruff]
line-length = 100

[tool.ruff.lint]
# Enable isort (I) for import sorting
select = ["I"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
