stream:
  path: "data/forest.mp4"
  #path: "0" # webcam
  #path: "https://www.youtube.com/watch?v=F0GOOP82094"
  quality: "bestvideo[height<=480]/best[height<=480]"

model:
  model_repo: "facebook/dinov3-vits16-pretrain-lvd1689m"
  device: "cuda"

detector:
  min_train_frames: 24
  min_anomaly_fraction: 0.02  # Trigger if >= 2% of patches are anomalous